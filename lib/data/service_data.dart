import 'package:wagon/data/users_data.dart';
import 'package:wagon/data/vehicle_data.dart';
import 'package:wagon/enums/route.dart';
import 'package:wagon/enums/service_state.dart';
import 'package:wagon/models/service.dart';

List<Service> allServices = [
  Service(
    id: '1',
    hour: DateTime.now(),
    waitingTime: DateTime.now(),
    places: 10,
    state: ServiceState.active,
    route: Route.route1,
    driver: usersData['1'],
    vehicle: vehiclesData['1'],
  ),
  Service(
    id: '2',
    hour: DateTime.now().add(Duration(hours: 1)),
    waitingTime: DateTime.now().add(Duration(hours: 1)),
    places: 10,
    state: ServiceState.finished,
    route: Route.route1,
    driver: usersData['2'],
    vehicle: vehiclesData['1'],
  ),
  Service(
    id: '3',
    hour: DateTime.now().add(Duration(hours: -1)),
    waitingTime: DateTime.now().add(Duration(hours: -1)),
    places: 10,
    state: ServiceState.active,
    route: Route.route2,
    driver: usersData['1'],
    vehicle: vehiclesData['2'],
  ),
  Service(
    id: '4',
    hour: DateTime.now().add(Duration(hours: -2)),
    waitingTime: DateTime.now().add(Duration(hours: -2)),
    places: 10,
    state: ServiceState.inactive,
    route: Route.route1,
    driver: usersData['2'],
    vehicle: vehiclesData['1'],
  ),
  Service(
    id: '5',
    hour: DateTime.now().add(Duration(days: 1)),
    waitingTime: DateTime.now().add(Duration(days: 1)),
    places: 10,
    state: ServiceState.active,
    route: Route.route1,
    driver: usersData['1'],
    vehicle: vehiclesData['2'],
  ),
  Service(
    id: '6',
    hour: DateTime.now().add(Duration(days: 1)),
    waitingTime: DateTime.now().add(Duration(days: 1)),
    places: 10,
    state: ServiceState.active,
    route: Route.route2,
    driver: usersData['2'],
    vehicle: vehiclesData['1'],
  ),
  Service(
    id: '7',
    hour: DateTime.now().add(Duration(hours: -2)),
    waitingTime: DateTime.now().add(Duration(hours: -2)),
    places: 10,
    state: ServiceState.finished,
    route: Route.route1,
    driver: usersData['2'],
    vehicle: vehiclesData['1'],
  ),
  Service(
    id: '8',
    hour: DateTime.now().add(Duration(minutes: -30)),
    waitingTime: DateTime.now().add(Duration(minutes: -30)),
    places: 10,
    state: ServiceState.finished,
    route: Route.route2,
    driver: usersData['1'],
    vehicle: vehiclesData['2'],
  ),
  Service(
    id: '9',
    hour: DateTime.now(),
    waitingTime: DateTime.now(),
    places: 10,
    state: ServiceState.inactive,
    route: Route.route2,
    driver: usersData['1'],
    vehicle: vehiclesData['1'],
  )
];
